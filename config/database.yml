development:
  adapter:  postgresql
  host:     localhost
  database: lp_api_dev
  username: postgres

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter:  postgresql
  host:     localhost
  database: lp-api_test
  username: postgres

cucumber:
  <<: *TEST

production:
  adapter:  postgresql
  host:     localhost
  database: lp_api_dev
  username: postgres